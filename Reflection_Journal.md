# Reflection Journal



Elda's Entry:



From this exercise, I learned that setting up the correct libraries is an important first step because it helps ensure that the code runs properly. Once the libraries are ready, the next step is to load the dataset and understand which features would help classify the dataset.

One question I have is whether it is useful to compare every pair of features to learn more about the data. I also want to understand how to determine which features are the most appropriate for classifying the dataset. Finally, I wonder whether a dataset can include too many features and how that might affect the analysis.


Michelle's Entry:

Going through the cells and running them one by one, seeing how the dataset went from raw numbers in a matrix to a table and then to an actual graph taught me a lot about how this whole process works with matplotlib (plt.). What stood out to me most was comparing the scatter plot to the mean values table — the table made it look like the three species were pretty different from each other, but once I saw the actual plot, versicolor and virginica overlapped a lot more than I thought they would.

Xinyu's Entry:
Analyzing data in a lab for this homework helped me to learn how pandas can be paired with NumPy when you are working with data. Initially, I knew that you could perform calculations with numbers in Python but I was unsure of the benefits of utilizing NumPy arrays as well as pandas DataFrames to organize the data for analysis of the data. After completing the Iris data exercises, I understand the benefits of the use of the two structures in order to organize the data as well as perform numerical calculations with the data.

In addition, running all the cells in Google Colab was a great learning experience for me. A notebook consists of more than just the final output of all the cells combined. In fact, each cell in a notebook typically relies on the output of the cells before it, so testing a whole notebook has to be different from testing individual functions, for example. I still have to learn more about the potential uses of the data we explored in this lab for building a machine learning model.
